@{
    ViewData["Title"] = "Registration Success";
}
<div class="d-flex justify-content-center align-items-center" style="min-height:60vh;">
  <div class="card shadow-lg border-0" style="max-width: 520px; width:100%;">
    <div class="card-body p-4 text-center">
      <h2 class="h4 mb-3">🎉 Account created</h2>
      <p class="text-muted mb-4">
        @(TempData["RegisterMessage"] ?? "Your account was created successfully.")
      </p>
      <div class="d-flex justify-content-center gap-2">
        <a class="btn btn-outline-secondary" asp-controller="Auth" asp-action="Login">Go to Login</a>
      </div>
    </div>
  </div>
</div>
