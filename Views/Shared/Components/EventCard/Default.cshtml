@model EventTicketingSystem.Models.EventCardVm



<div class="card border-0 shadow-lg h-100">
    <div class="card-body d-flex flex-column">
        <div class="d-flex justify-content-between align-items-start mb-1">
            <h3 class="h5 card-title mb-0">@Model.Title</h3>
            <span class="badge text-bg-light">Available: @Model.Availability</span>
        </div>
        <div class="text-muted small mb-2">@Model.DateTime • @Model.Venue</div>
        <div class="fw-semibold mb-3">@Model.Price</div>
        <div class="mt-auto d-flex gap-2">
            <a class="btn btn-outline-secondary" href="/events/details/1">View Details</a>
            <a class="btn btn-dark" href="/checkout">Book Now</a>
        </div>
    </div>
</div>
